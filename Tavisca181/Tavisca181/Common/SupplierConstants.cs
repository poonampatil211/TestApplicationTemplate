using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Tavisca181.Common
{
    public class SupplierConstants
    {
        public static class ConfigurationKeys
        {
            // Change the TestUrlKey if supplier Url is not on this key
            public static readonly string TestUrlKey = "configurationsettings:SupplierSettings:TestUrl";
        }
    }
}
